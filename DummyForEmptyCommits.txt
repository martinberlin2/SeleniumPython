git commit -m "Why commit before pull?"

neue sitzung:
wen pull fails:

erst commit, KEIN push, 
dann pull der sagt conflicts an

--- das hier kommt nach develop, commit, push, dann nach cfg-dev, und develop reinmergen